# core (classical pipeline — required)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# real PhysioNet data loading
wfdb>=4.1

# deep-learning arm (optional — only needed for fecg.dl)
torch>=2.0

# optional: sanity-check detectors / HRV baselines
neurokit2>=0.2

# interactive test bench (app.py)
streamlit>=1.40
pandas>=2.0
matplotlib>=3.9
